# Primary (writes) — CQRS command side
POSTGRES_USER=fintech
POSTGRES_PASSWORD=change-me-primary-strong
POSTGRES_DB=fintech

# Replication user password (must match on all nodes)
POSTGRES_REPLICATION_PASSWORD=change-me-replication-strong

# Host ports (optional)
POSTGRES_PRIMARY_PORT=5432
POSTGRES_READ1_PORT=5433
POSTGRES_READ2_PORT=5434
